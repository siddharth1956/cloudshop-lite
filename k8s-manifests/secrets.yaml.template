# RENAME to secrets.yaml and fill in values
# secrets.yaml is in .gitignore — NEVER commit real secrets
apiVersion: v1
kind: Secret
metadata:
  name: cloudshop-secrets
  namespace: default
type: Opaque
stringData:
  DB_PASSWORD: "change-me"
  JWT_SECRET: "change-me-to-a-long-random-string"
